<!DOCTYPE html>
<html lang="ja">
<head>
  <title>Juni</title>
  <meta charset="utf-8">
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=1024">
  <!-- <meta name="viewport" content="width=device-width,initial-scale=1.0,minimum-scale=1.0,maximum-scale=1.0,user-scalable=no"> -->
  <%= stylesheet_link_tag    'application' %>
  <%= javascript_include_tag 'application' %>
</head>
<body>
  <header class="l-header">
    <div class="global-header">
      <h1 class="global-header__heading">
        <%= link_to root_path do %>
          <%= image_tag 'logo.jpg', width: '100' %>juni interior design &amp; work space
        <% end %>
      </h1>
      <nav class="global-header__nav">
        <ul class="global-nav">
          <li><%= category_path(:interior) %></li>
          <li><%= category_path(:shops) %></li>
          <li><%= category_path(:apparel) %></li>
          <li><%= category_path(:music) %></li>
          <li><%= category_path(:photo) %></li>
          <li><%= category_path(:movie) %></li>
          <li><%= category_path(:other) %></li>
          <li><%= link_to 'contact', '#' %></li>
        </ul>
      </nav>
    </div>
  </header>
  <div class="l-container">
    <%= yield %>
  </div>
  <footer class="l-footer">
  </footer>
</body>
</html>
