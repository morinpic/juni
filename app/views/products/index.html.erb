<% @products.each do |p| %>
  <p>
    <%= p.created_at.strftime('%Y.%m.%d') %>
  </p>
  <h2><%= p.title %></h2>
  <p><%= p.body %></p>
  <% p.pictures.each do |pic| %>
    <%= image_tag pic.image.url("medium") %>
  <% end %>
  <%= link_to "show", p %>
<% end %>
